@page
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model WebApplication1.Pages.SuppliersModel
@{
}
Neuen Lieferanten hinzufügen
<div class="row">
    <p>Enter a name for a new supplier:</p>
    <form method="POST">
        <div><input asp-for="Supplier.CompanyName" /></div>
        <div><input asp-for="Supplier.Address" /></div>
        <div><input asp-for="Supplier.City" /></div>
        <input type="submit" />
    </form>
</div>
<div class="row">
    <h1 class="display-2">Suppliers</h1>
    <table class="table">
        <thead class="thead-inverse">
            <tr><th>Company Name</th></tr>
        </thead>
        <tbody>
            @foreach (string name in Model.Suppliers)
            {
                <tr><td>@name</td></tr>
            }
        </tbody>
    </table>
</div>
